#!/bin/bash

set -e


if [ `whoami` != "root" ]; then 
	echo You must run install.sh as root
	exit 126
fi

# create the config from the .example
cp $OPENVPN/server.conf.example $OPENVPN/server.conf

#if [ -z "$LDAP" ]; then
#	echo Not using LDAP auth
#else
#	# add the ldap auth plugin
#	echo "plugin /usr/lib/openvpn/openvpn-auth-ldap.so $OPENVPN/auth/auth-ldap.conf" >> $OPENVPN/server.conf
#fi

#if [ -z "$ROUTE" ]; then
#	echo Not pushing any routes
#	#export ROUTE="172.17.0.0 255.255.0.0"
#else
#	echo Pushing the route to the server config
#	echo -e "\npush \"route $ROUTE\"" >> $OPENVPN/server.conf
#fi

